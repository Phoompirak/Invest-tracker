# 📊 Invest Tracker - ระบบจัดการพอร์ตการลงทุน

<div align="center">

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Thai](https://img.shields.io/badge/language-ไทย-red.svg)

**ระบบติดตามพอร์ตการลงทุนครบวงจร รองรับหุ้น กองทุน ทองคำ และสินทรัพย์ทางเลือก**

[🚀 เริ่มใช้งาน](#-วิธีติดตั้งแบบละเอียด-สำหรับผู้เริ่มต้น) • [✨ ฟีเจอร์](#-ฟีเจอร์หลัก) • [📱 วิธีใช้งาน](#-วิธีใช้งานเบื้องต้น)

</div>

---

## ✨ ฟีเจอร์หลัก

| ฟีเจอร์ | รายละเอียด |
|---------|-----------|
| 📈 **ติดตามพอร์ต** | ดูมูลค่าสินทรัพย์ทั้งหมด กำไร/ขาดทุน แบบ Real-time |
| 💰 **รองรับหลายสินทรัพย์** | หุ้นไทย, หุ้นต่างประเทศ, กองทุน, ทองคำ (MTS-GOLD) |
| 🔄 **สลับสกุลเงิน** | THB ↔ USD พร้อมอัตราแลกเปลี่ยนอัตโนมัติ |
| 📊 **กราฟวิเคราะห์** | แสดงสัดส่วนพอร์ต, P&L ตามช่วงเวลา |
| ⚡ **คำนวณกำไร FIFO** | คำนวณกำไร/ขาดทุนอัตโนมัติเมื่อขาย |
| ☁️ **Sync Google Sheets** | บันทึกข้อมูลไปยัง Google Sheets อัตโนมัติ |
| 🌙 **Dark / Light Mode** | ปรับธีมตามความชอบ |
| 📱 **ใช้งานบนมือถือได้** | Responsive Design รองรับทุกหน้าจอ |

---

## 🚀 วิธีติดตั้งแบบละเอียด (สำหรับผู้เริ่มต้น)

> 💡 **ไม่ต้องมีความรู้เรื่องการเขียนโค้ดมาก่อน** ทำตามขั้นตอนทีละข้อได้เลย!

### 📋 สิ่งที่ต้องเตรียม

1. **คอมพิวเตอร์** (Windows, Mac, หรือ Linux)
2. **อินเทอร์เน็ต**
3. **บัญชี Google** (สำหรับ Sync ข้อมูล)

---

### ขั้นตอนที่ 1: ติดตั้ง Node.js 🟢

Node.js คือโปรแกรมที่ช่วยให้เว็บแอปทำงานได้

1. ไปที่ **[https://nodejs.org](https://nodejs.org)**
2. กดดาวน์โหลดปุ่มสีเขียว **"LTS"** (แนะนำ)
3. เปิดไฟล์ที่ดาวน์โหลดมา แล้วกด **Next → Next → Install**
4. รอจนติดตั้งเสร็จ กด **Finish**

**✅ ตรวจสอบว่าติดตั้งสำเร็จ:**
- เปิด **Command Prompt** (พิมพ์ `cmd` ในช่องค้นหา Windows)
- พิมพ์คำสั่งนี้แล้วกด Enter:
  ```
  node --version
  ```
- ถ้าเห็นตัวเลขเช่น `v20.x.x` แสดงว่าสำเร็จแล้ว! ✅

---

### ขั้นตอนที่ 2: ดาวน์โหลดโปรเจค 📥

**วิธี A: ดาวน์โหลดเป็นไฟล์ ZIP (ง่ายที่สุด)**

1. ไปที่หน้า GitHub ของโปรเจคนี้
2. กดปุ่มสีเขียว **"Code"** → **"Download ZIP"**
3. แตกไฟล์ ZIP ไปยังโฟลเดอร์ที่ต้องการ (เช่น `C:\Projects\Invest-tracker`)

**วิธี B: ใช้ Git (สำหรับคนที่ใช้ Git เป็น)**
```bash
git clone https://github.com/YOUR_USERNAME/Invest-tracker.git
cd Invest-tracker
```

---

### ขั้นตอนที่ 3: ติดตั้ง Dependencies 📦

1. เปิด **Command Prompt** หรือ **Terminal**
2. เข้าไปในโฟลเดอร์โปรเจค:
   ```bash
   cd C:\Projects\Invest-tracker
   ```
   (เปลี่ยน path ตามที่คุณแตกไฟล์ไว้)

3. พิมพ์คำสั่งนี้แล้วกด Enter:
   ```bash
   npm install
   ```
4. รอสักครู่ (ประมาณ 1-3 นาที) จนเห็นข้อความ `added xxx packages`

---

### ขั้นตอนที่ 4: ตั้งค่า Google API (สำหรับ Sync ข้อมูล) 🔐

> ⚠️ ขั้นตอนนี้จำเป็นสำหรับการ Sync ข้อมูลไป Google Sheets

#### 4.1 สร้าง Google Cloud Project

1. ไปที่ **[Google Cloud Console](https://console.cloud.google.com)**
2. ล็อกอินด้วย Google Account
3. กด **"Select a project"** (มุมบนซ้าย) → **"NEW PROJECT"**
4. ตั้งชื่อ Project (เช่น `Invest Tracker`) → กด **CREATE**

#### 4.2 เปิดใช้งาน Google Sheets API

1. ในหน้า Google Cloud Console ค้นหา **"Google Sheets API"**
2. กด **ENABLE**
3. ค้นหา **"Google Drive API"** แล้วกด **ENABLE** เช่นกัน

#### 4.3 สร้าง API Credentials

1. ไปที่ **APIs & Services** → **Credentials**
2. กด **"+ CREATE CREDENTIALS"** → **"OAuth client ID"**
3. เลือก **"Configure consent screen"** ก่อน:
   - เลือก **External** → กด **CREATE**
   - กรอก **App name:** `Invest Tracker`
   - กรอก **User support email:** อีเมลของคุณ
   - กรอก **Developer contact email:** อีเมลของคุณ
   - กด **SAVE AND CONTINUE** ไปจนจบ
4. กลับมา **Credentials** → **"+ CREATE CREDENTIALS"** → **"OAuth client ID"**
5. เลือก **Application type:** `Web application`
6. ตั้งชื่อ: `Invest Tracker Web`
7. ในส่วน **Authorized JavaScript origins** กด **"+ ADD URI"**:
   - ใส่: `http://localhost:5173`
   - ใส่อีกอัน: `http://localhost:8080`
8. กด **CREATE**
9. จะได้ **Client ID** หน้าตาประมาณนี้:
   ```
   123456789-xxxxxxxxxx.apps.googleusercontent.com
   ```

#### 4.4 สร้างไฟล์ .env

1. ในโฟลเดอร์โปรเจค สร้างไฟล์ชื่อ `.env` (ไม่มีนามสกุล)
2. เปิดไฟล์ด้วย Notepad แล้วใส่:
   ```env
   VITE_GOOGLE_CLIENT_ID=YOUR_CLIENT_ID_HERE
   VITE_GOOGLE_API_KEY=YOUR_API_KEY_HERE
   ```
3. แทนที่ `YOUR_CLIENT_ID_HERE` ด้วย Client ID ที่ได้จากขั้นตอน 4.3

> 💡 **ไม่มี API Key?** สร้างได้ที่ **Credentials** → **"+ CREATE CREDENTIALS"** → **"API key"**

---

### ขั้นตอนที่ 5: รันแอปพลิเคชัน 🎉

1. ใน Command Prompt/Terminal พิมพ์:
   ```bash
   npm run dev
   ```
2. รอจนเห็นข้อความ:
   ```
   ➜  Local:   http://localhost:5173/
   ```
3. **เปิดเบราว์เซอร์** แล้วไปที่ **http://localhost:5173**
4. 🎊 **เสร็จแล้ว!** เว็บจะแสดงหน้าล็อกอิน

---

## 📱 วิธีใช้งานเบื้องต้น

### 1. ล็อกอินด้วย Google
- กดปุ่ม **"Sign in with Google"**
- เลือกบัญชี Google ที่ต้องการใช้
- อนุญาตให้แอปเข้าถึง Google Sheets

### 2. เพิ่มรายการซื้อ
1. ไปที่แท็บ **"รายการซื้อขาย"**
2. เลือกประเภท **"ซื้อ"**
3. กรอกข้อมูล:
   - **ชื่อหุ้น/สินทรัพย์:** เช่น `PTT`, `MTS-GOLD`, `AAPL`
   - **จำนวน:** จำนวนหุ้นหรือหน่วย
   - **ราคา:** ราคาต่อหุ้น/หน่วย
4. กด **"บันทึก"**

### 3. เพิ่มรายการขาย
1. เลือกประเภท **"ขาย"**
2. กรอกข้อมูลเหมือนการซื้อ
3. ระบบจะ **คำนวณกำไร/ขาดทุนอัตโนมัติ (FIFO)**

### 4. ดูรายละเอียดสินทรัพย์
- ในหน้า **"สินทรัพย์ของฉัน"** กดที่ชื่อสินทรัพย์
- จะเปิดหน้าต่างแสดงรายละเอียด:
  - ทุนที่ลงไป
  - กำไรที่ขายออกไปแล้ว
  - ประวัติการซื้อขาย

### 5. สลับธีม 🌙/☀️
- กดไอคอน **ดวงอาทิตย์/พระจันทร์** ที่มุมขวาบน
- เลือก Light, Dark, หรือ System

---

## 🔧 การแก้ปัญหาเบื้องต้น

| ปัญหา | วิธีแก้ |
|-------|--------|
| `npm` command not found | ติดตั้ง Node.js ใหม่ แล้ว Restart คอมพิวเตอร์ |
| หน้าเว็บขาว/ไม่โหลด | เปิด Developer Tools (F12) ดู Console ว่ามี Error อะไร |
| Sync ไม่ทำงาน | ตรวจสอบไฟล์ `.env` ว่าใส่ API Key ถูกต้อง |
| ราคาทองไม่อัพเดท | ใช้ชื่อ Ticker ที่มีคำว่า `GOLD` เช่น `MTS-GOLD` |

---

## 🛠 เทคโนโลยีที่ใช้

- **Frontend:** React 18, TypeScript, Tailwind CSS
- **UI Components:** shadcn/ui, Radix UI
- **Charts:** Recharts
- **State Management:** TanStack Query
- **Build Tool:** Vite
- **Theme:** next-themes

---

## 📄 License

MIT License - สามารถนำไปใช้และแก้ไขได้อย่างอิสระ

---

## 🙏 ขอบคุณ

สร้างด้วย ❤️ สำหรับนักลงทุนไทยทุกคน

หากมีคำถามหรือข้อเสนอแนะ สามารถเปิด Issue ได้เลยครับ!
